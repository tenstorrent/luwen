[package]
name = "luwen-core"
version = "0.2.0"
description = "Common dependencies shared by all crates based on luwen"
edition = "2021"
license = "Apache-2.0"

[dependencies]
thiserror = "1.0.40"
serde = {version = "1.0.185", features = ["derive"]}
