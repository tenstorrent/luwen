[package]
name = "drunken-monkey"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = {version = "4.4.14", features = ["derive"]}

luwen-if = {path = "../luwen-if"}
luwen-ref = {path = "../luwen-ref"}
