[package]
name = "drunken-monkey"
version = "0.7.10"
edition = "2021"

[dependencies]
clap = { version = "4.4.14", features = ["derive"] }
luwen = { workspace = true }

[package.metadata.workspaces]
independent = true
